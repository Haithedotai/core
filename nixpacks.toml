[phase.name]
aptPkgs = ['unzip']

[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = [
    "apt update",
    "apt upgrade",
    "apt install unzip",
    "bun upgrade",
    "bun install",
]

[phases.build]
cmds = ["bun run server:build"]

[start]
cmd = "bun run packages/server/prod.ts"
